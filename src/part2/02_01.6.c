/* Fabian, Insert date here */
# include <stdio.h>

int main()
{
    int a = -5;
    
    if (a > 0)     
    /* The parentheses or semi-colon go after the statement not
     * not the parentheses which hold the if condition.
    */
        printf("%d is a positive number. \n", a);

    return(0);
}
