/* Fabian, Insert date here */
# include <stdio.h>

/*  My verison differs as we are not using the modulus operator
 *  the for statement can be modified in oneline to print even numbers
 *  up to 20.
 * */

int main()
{
    int x;
    
    for(x=2; x<=20; x+2)
        printf("%d \n", x);

    return(0);
}
