/* Fabian, Insert date here */
# include <stdio.h>

/* 
 * The code shows "int a" without initialization. We have declare that
 * this is of type int however have not assign a value.
 *
 * Shown in the next code is this example with initialization.
 * */

int main()
{
    int a;

    printf("The value of a is %d\n", a);

    return(0);
}
