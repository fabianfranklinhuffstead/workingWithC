/* Fabian, Insert date here */
# include <stdio.h>

int main()
{
    float a;

    printf("A float variable occupies %lu bytes of storage \n",sizeof(a));

    /* 
     * Here the %lu means a long unsigned integer value.
     * */
    return(0);
}
