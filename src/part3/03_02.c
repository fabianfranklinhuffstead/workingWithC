/* Fabian, Insert date here */
# include <stdio.h>
# include <string.h>
int main()
{
    char string[] = "Just how long am I?";
    int len;

    len = strlen(string); // string length number of characters in a string
    printf("The following string: \n");
    puts(string);
    printf("is %d character long. \n", len); //string len print here

    return(0);
}
