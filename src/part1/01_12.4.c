/* Fabian, Insert date here */
 # include <stdio.h>

int main()
{
    int a,b,c;

    printf("Type three letters: ");
    a = getchar();
    putchar(a);

    b = getchar();
    putchar(b);

    c = getchar();
    putchar(c);

    /* Please note input/output methods are stream orientated. */
    return(0);
}
