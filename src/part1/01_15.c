/* Fabian, Insert date here */
# include <stdio.h>
# include <math.h>

int main()
{
    float r;

    r = sqrt(2.0); /* sqrt is a function provided by the math header library */

    printf("The square root of 2 is %f\n",r);
    
    return(0);
}
